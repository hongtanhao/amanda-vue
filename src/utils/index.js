export const winClientWidth = window
  ? window.innerWidth
    ? window.innerWidth
    : window.document.innerWidth
  : '100%'
export const winClientHeight = window
  ? window.innerHeight
    ? window.innerHeight
    : window.document.innerHeight
  : '100%'
